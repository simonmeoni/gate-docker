# gate teamware server
a docker for deploying a gate server

# How it's works ?
execute `sudo ./build.sh`
or : 

1. `echo "HOST=localhost" > .env`
2. `sudo build -t gate ./gate`
3. `sudo docker-compose up`
